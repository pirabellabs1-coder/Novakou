// Mentors share the exact same settings surface as vendeurs (compte,
// nom de domaine, paiements, notifications, sécurité, coaching). Any
// instructeur is also a mentor in our model, so re-using the vendor page
// keeps a single source of truth — including the custom domain feature.
export { default } from "@/app/(formations-dashboard)/vendeur/parametres/page";
