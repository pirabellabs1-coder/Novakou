/**
 * WebRTC call sounds stub — kept for messaging call components.
 */

function noop() {}

export const playRingSound = noop;
export const stopRingSound = noop;
export const playCallEndSound = noop;
export const playMissedCallSound = noop;
export const playConnectedSound = noop;
