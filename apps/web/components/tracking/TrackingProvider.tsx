"use client";

/**
 * Stub — TrackingProvider.
 * Placeholder for analytics/tracking context. No-op in current version.
 */

export function TrackingProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
